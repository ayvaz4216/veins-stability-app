This repository includes code for stability checking routing program.
The scenerio files are in the veins/examples/ileriag folder
Application logic(StabilityApp.cc StabilityMessage.msg StabilityApp.h StabilityApp.ned) is included in the traci example code. veins/src/veins/modules/application/traci

The algorithm looks for relative speed and rejects high relative speeds.
